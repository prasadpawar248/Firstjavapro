package javaCollections;

public class CollectionMethod 
{
	public static void main(String[] args) {
		
		
		//a.add(); to add element
		//a.addAll(collection b); to add all element into b collection
		//a.remove("element");
		//a.removeAll(collection b); // to remove all element from collection a which present b collection.
		//a.retainAll(collection b); to retain element from a which are available in b.
		//a.isEmpty(); // true
		//a.size(); to find size
		//a.contains("element");  true
		//a.containsAll(collection b); true
		//a.clear();
		//a.toArray(collection b);to convert collection b to array.
	}

}
